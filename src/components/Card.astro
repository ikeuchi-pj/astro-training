---
const { href, title, url, createdAt, contents } = Astro.props;
---

<div class="card">
  <a href={`/${href}`}>
    <h2>{title}</h2>
    <p>{createdAt}</p>
    <p set:html={contents}></p>
  </a>
</div>